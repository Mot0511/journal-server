# Journal Backend API Documentation

## Обзор

Это RESTful API для системы электронного журнала, поддерживающий функционал для студентов и преподавателей. API построен на Node.js с использованием Express.js и PostgreSQL.

## Базовый URL

```
http://localhost:3000/api
```

## Аутентификация

API использует JWT (JSON Web Tokens) для аутентификации. После успешного логина вы получите токен, который нужно включать в заголовок Authorization для всех защищенных запросов.

```
Authorization: Bearer YOUR_JWT_TOKEN
```

## Структура ответов

Все ответы имеют следующую структуру:

```json
{
  "success": true/false,
  "message": "Описание результата",
  "data": {...} // Данные ответа (если есть)
}
```

## Endpoints

### 🔐 Аутентификация (/api/auth)

#### POST /auth/login/student
Авторизация студента

**Тело запроса:**
```json
{
  "login": "student_login",
  "password": "password"
}
```

**Ответ:**
```json
{
  "success": true,
  "message": "Авторизация успешна",
  "data": {
    "user": {
      "id": 1,
      "Login": "student_login",
      "FirstName": "Иван",
      "LastName": "Иванов",
      "MiddleName": "Иванович",
      "GroupId": 1,
      "group_title": "ИВТб-1301-05-00"
    },
    "token": "jwt_token_here",
    "role": "student"
  }
}
```

#### POST /auth/login/teacher
Авторизация преподавателя

**Тело запроса:**
```json
{
  "login": "teacher_login",
  "password": "password"
}
```

#### GET /auth/me
Получить информацию о текущем пользователе (требует аутентификации)

#### POST /auth/change-password
Изменить пароль (требует аутентификации)

**Тело запроса:**
```json
{
  "currentPassword": "old_password",
  "newPassword": "new_password"
}
```

#### POST /auth/logout
Выход из системы (требует аутентификации)

---

### 👨‍🎓 Студенты (/api/students)

#### GET /students/me
Получить информацию о текущем студенте (только для студентов)

#### GET /students
Получить всех студентов (только для преподавателей)

#### GET /students/:id
Получить студента по ID (только для преподавателей)

#### GET /students/:id/full
Получить полную информацию о студенте с активностями и занятиями (только для преподавателей)

#### GET /students/group/:groupId
Получить студентов по группе (только для преподавателей)

#### POST /students
Создать нового студента (только для преподавателей)

**Тело запроса:**
```json
{
  "login": "new_student",
  "password": "password",
  "lastName": "Фамилия",
  "firstName": "Имя",
  "middleName": "Отчество",
  "groupId": 1,
  "vkId": "vk_id",
  "tgId": "tg_id",
  "vyatsuMail": "email@vyatsu.ru"
}
```

---

### 👨‍🏫 Преподаватели (/api/teachers)

#### GET /teachers/me
Получить информацию о текущем преподавателе (только для преподавателей)

#### GET /teachers
Получить всех преподавателей (только для преподавателей)

#### GET /teachers/:id/subjects
Получить предметы и группы преподавателя

#### GET /teachers/:id/students
Получить студентов преподавателя по предмету

**Параметры запроса:**
- `subjectId` (обязательный) - ID предмета
- `groupId` (опциональный) - ID группы

---

### 👥 Группы (/api/groups)

#### GET /groups
Получить все группы

#### GET /groups/:id
Получить группу по ID

#### GET /groups/:id/students
Получить группу со студентами

#### GET /groups/:id/subjects
Получить предметы группы

#### GET /groups/:id/stats
Получить статистику группы

#### POST /groups
Создать новую группу (только для преподавателей)

**Тело запроса:**
```json
{
  "title": "ИВТб-1301-05-00",
  "number": "1301"
}
```

---

### 📊 Оценки и Активности (/api/grades)

#### Активности

#### GET /grades/activities
Получить все активности (с пагинацией)

**Параметры запроса:**
- `limit` - количество записей (по умолчанию 50)
- `offset` - смещение (по умолчанию 0)

#### GET /grades/activities/journal
Получить активности для журнала (только для преподавателей)

**Параметры запроса:**
- `teacherId` (обязательный) - ID преподавателя
- `subjectId` (обязательный) - ID предмета
- `groupId` (опциональный) - ID группы

#### GET /grades/activities/student/:studentId
Получить активности студента

#### POST /grades/activities
Создать новую активность (только для преподавателей)

**Тело запроса:**
```json
{
  "studentId": 1,
  "subjectId": 1,
  "teacherId": 1,
  "taskId": 1,
  "taskTypeId": 1,
  "meta": "Дополнительная информация",
  "date": "2023-10-30",
  "mark": "5"
}
```

#### Занятия

#### GET /grades/lessons/journal
Получить занятия для журнала (только для преподавателей)

**Параметры запроса:**
- `subjectId` (обязательный) - ID предмета
- `groupId` (опциональный) - ID группы
- `typeSubjectId` (опциональный) - ID типа занятия

#### POST /grades/lessons
Создать новое занятие (только для преподавателей)

**Тело запроса:**
```json
{
  "studentId": 1,
  "subjectId": 1,
  "typeSubject": 1,
  "mark": "Н",
  "date": "2023-10-30"
}
```

#### POST /grades/lessons/bulk
Массовое создание занятий для группы (только для преподавателей)

**Тело запроса:**
```json
{
  "groupId": 1,
  "subjectId": 1,
  "typeSubjectId": 1,
  "date": "2023-10-30",
  "defaultMark": ""
}
```

#### Статистика

#### GET /grades/stats/attendance/:studentId
Получить статистику посещаемости студента

**Параметры запроса:**
- `subjectId` (опциональный) - ID предмета

#### GET /grades/stats/student/:studentId
Получить статистику активностей студента

---

### 📚 Предметы (/api/subjects)

#### GET /subjects
Получить все предметы

#### GET /subjects/:id
Получить предмет по ID

#### GET /subjects/:id/teachers
Получить преподавателей предмета

#### GET /subjects/:id/groups
Получить группы, изучающие предмет

#### POST /subjects
Создать новый предмет (только для преподавателей)

**Тело запроса:**
```json
{
  "title": "Информатика"
}
```

---

## Интеграция с фронтендом

### Настройка клиента

1. Установите базовый URL для API:
```javascript
const API_BASE_URL = 'http://localhost:3000/api';
```

2. Создайте функцию для авторизованных запросов:
```javascript
const apiRequest = async (endpoint, options = {}) => {
  const token = localStorage.getItem('token');
  
  const config = {
    headers: {
      'Content-Type': 'application/json',
      ...(token && { Authorization: `Bearer ${token}` }),
      ...options.headers,
    },
    ...options,
  };

  const response = await fetch(`${API_BASE_URL}${endpoint}`, config);
  return response.json();
};
```

### Примеры использования

#### Авторизация студента:
```javascript
const login = async (login, password, userType) => {
  const response = await fetch(`${API_BASE_URL}/auth/login/${userType}`, {
    method: 'POST',
    headers: {
      'Content-Type': 'application/json',
    },
    body: JSON.stringify({ login, password }),
  });
  
  const data = await response.json();
  
  if (data.success) {
    localStorage.setItem('token', data.data.token);
    localStorage.setItem('user', JSON.stringify(data.data.user));
    localStorage.setItem('role', data.data.role);
  }
  
  return data;
};
```

#### Получение студентов группы:
```javascript
const getGroupStudents = async (groupId) => {
  return await apiRequest(`/students/group/${groupId}`);
};
```

#### Создание новой активности:
```javascript
const createActivity = async (activityData) => {
  return await apiRequest('/grades/activities', {
    method: 'POST',
    body: JSON.stringify(activityData),
  });
};
```

#### Получение журнала:
```javascript
const getJournalData = async (teacherId, subjectId, groupId) => {
  const params = new URLSearchParams({
    teacherId,
    subjectId,
    ...(groupId && { groupId }),
  });
  
  return await apiRequest(`/grades/activities/journal?${params}`);
};
```

### Обработка ошибок

```javascript
const handleApiError = (response) => {
  if (!response.success) {
    if (response.message === 'Недействительный токен') {
      // Перенаправить на страницу логина
      localStorage.clear();
      window.location.href = '/login';
    } else {
      // Показать сообщение об ошибке
      alert(response.message);
    }
  }
  return response;
};
```

---

## Безопасность

- Все пароли хешируются с использованием bcrypt
- JWT токены истекают через 24 часа
- Применяются ограничения по количеству запросов (rate limiting)
- Используется CORS для ограничения доменов
- Валидация всех входящих данных

## Переменные окружения

Создайте файл `.env` в папке backend:

```
DB_HOST=localhost
DB_PORT=5432
DB_NAME=prpjectAS
DB_USER=postgres
DB_PASSWORD=sisisisisi123
JWT_SECRET=your_secret_key_here
JWT_EXPIRES_IN=24h
PORT=3000
NODE_ENV=development
```

## Запуск

1. Установите зависимости:
```bash
cd backend
npm install
```

2. Запустите сервер:
```bash
npm run dev
```

API будет доступен по адресу: http://localhost:3000